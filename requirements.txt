absl-py>=1.0.0
numpy>=1.22.0,<1.24.0 # <1.24.0 for TF 2.15 compatibility with some systems
opencv-python>=4.6.0
tensorflow==2.15.0
tensorflow-addons==0.22.0 # Or remove if not using its dense_image_warp/gaussian_filter2d
Pillow>=9.0.0 # For image I/O if used by dataloader
# tf_slim (can likely be removed for COMISR if not used elsewhere)
# protobuf < 4.0.0 is often needed for older TF versions, TF 2.15 might be more flexible
protobuf>=3.20.0,<5.0.0 # Broader range for TF 2.15